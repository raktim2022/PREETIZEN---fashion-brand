export const metadata = {
  title: 'Returns & Exchange Policy | Preetizen',
  description: 'Learn about Preetizen\'s returns, exchanges, and refund policies.',
};

export default function ReturnsLayout({ children }) {
  return children;
}